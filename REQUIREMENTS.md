# 腰痛 - 健康提醒 Mac 应用 产品需求

> 本文档只描述**产品需求**：要做什么、给谁用、什么体验。
> 技术实现、架构选型、文件结构、构建说明见 [TECHNICAL.md](TECHNICAL.md)。

---

## 1. 项目目标

打造一个 macOS 健康提醒应用，帮助长时间办公的用户在持续工作一段时间后主动起身活动，预防久坐带来的健康问题（颈肩腰背、眼睛疲劳等）。

**应用名：腰痛** —— 直白点出久坐对腰部健康的危害，警示感更强。

---

## 2. 核心思路

应用根据用户的**输入活动**（鼠标、键盘、滚轮、触摸板）来判断"工作 / 休息"状态：当用户持续工作达到阈值后，菜单栏图标变红提醒站起来活动；休息一段时间后再次归零。

### 2.1 可识别的活动信号

| 信号 | 描述 |
|------|------|
| 鼠标点击 | 左/右/中键按下 |
| 鼠标拖拽 | 按住键拖动 |
| 鼠标移动 | 鼠标位置变化 |
| 键盘按键 | 任意键按下 |
| 修饰键 | Shift / Ctrl / Opt / Cmd / Caps Lock |
| 系统键 | 媒体键 / 亮度键 / 音量键 |
| 滚轮滚动 | 上下/左右滚动 |
| 触摸板手势 | 多指滑动、捏合等 |

> 屏幕关闭 / 系统休眠 = 用户已离开工位，被视为**已充分休息**。检测方式：wall-clock gap（两次 tick 间隔 >2s）+ `NSWorkspace.didWakeNotification` 双路并用，互为兜底。唤醒后工作计时清 0 + 挂"等待活动"门——必须**下一次输入**才重新开始计时。

### 2.2 工作 / 休息判定逻辑

应用内部维护两个独立的计时器：

| 计时器 | 含义 | 怎么涨 |
|---|---|---|
| **工作计时** | "自上次休息以来的墙钟时长" | **每秒钟 +1**（不依赖活动） |
| **休息计时** | "自上次活动以来的空闲时长" | 用户最近 1 秒内无任何输入时累加（活动时归零） |

判定规则：

- **休息判定**：休息计时达到 N 分钟（默认 10 分钟）→ **工作计时清 0**，并进入"等待活动"状态。
- **等待活动**：处于该状态时，**工作计时一直保持为 0**，不论墙钟过了多久；只有用户**下一次输入**（鼠标/键盘）才释放门，**下一 tick** 重新开始累加。
- **超时判定**：工作计时达到阈值（默认 30 分钟）→ 进入"超时"状态，菜单栏图标变红。

> 一句话总结：工作计时是**墙钟**（鼠标不动也累加），但休息判定命中后会被**暂停**（工作清 0 + 挂等待门），等用户回来才继续。

### 2.3 信号采集方案 ✅ 极简

只调用 macOS 系统自带的 `CGEventSource.secondsSinceLastEventType` 查询"距离上次任意输入多少秒"，不安装任何全局事件监听器。

- 不需要计算鼠标移动量
- 不需要申请辅助功能（Accessibility）权限
- 不污染用户的事件流
- 不区分活动类型——判定只看 `idleSeconds < 1.0`

---

## 3. 核心参数（默认值，可配置）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 工作时长阈值 | 30 分钟 | 累计多久后进入"超时"状态 |
| 休息时长阈值 | 10 分钟 | 空闲多久后重置工作计时器 |
| "活动"判定 | 最近 1 秒内有输入 | 决定休息计时是否累加 |

> 工作/休息时长在主界面和菜单里都能调整：
> - 工作：**2 / 5 / 10 / 15 / 20 / 30 / 45 / 60** 分钟（含 2 分钟用于快速测试）
> - 休息：**1 / 2 / 5 / 10 / 15 / 20 / 30 / 45 / 60** 分钟（含 1、2 分钟用于快速测试）

---

## 4. 功能需求

### 4.1 必须功能（P0）

- [x] **空闲时间检测**：自动识别用户是否在使用电脑
- [x] **工作计时**（墙钟）：每秒钟 +1，受"等待活动"门控制
- [x] **休息计时**（空闲）：无活动时累加，活动时归零
- [x] **休息判定**：休息计时 ≥ 阈值 → 清 0 工作计时 + 挂"等待活动"门
- [x] **超时判定**：工作计时 ≥ 阈值 → 进入"超时"状态
- [x] **菜单栏图标**：常驻 macOS 顶部菜单栏
  - 工作中：**白色** `circle.fill`（18pt SF Symbol）
  - 超时：**红色** `circle.fill`
  - **不做任何文字/数字/计时显示**，仅靠颜色变化
- [x] **主界面窗口**：应用启动后自动弹出
  - 顶部：**工作计时**（40pt 大字，`MM:SS / MM:SS` + 进度条）
  - 中部：**休息计时**（28pt，`MM:SS / MM:SS` + 进度条）
  - 设置区：工作/休息时长 Picker、状态区
  - **重启 腰痛** + 退出 腰痛 按钮
  - 打开时 Dock 显示蓝色圆图标（构建时烧进 bundle 的 PNG），关闭后回到纯菜单栏模式（不占 Dock）
- [x] **菜单栏下拉菜单**（点击图标弹出）
  - 第一项：显示主界面
  - 工作时长子菜单
  - 休息时长子菜单
  - 暂停腰痛 / 开始腰痛（纯开关）
  - 重启 腰痛
  - 退出 腰痛
- [x] **配置持久化**：用户设置重启后保留（`UserDefaults`）

### 4.2 增强功能（P1）

- [x] **暂停监控**："暂停腰痛 / 开始腰痛" 开关（会议场景）
  - 暂停期间不计时、不变色、不检测活动
  - 纯手动开关，无自动恢复（用户回来后点"开始腰痛"恢复）
  - **不持久化**：每次启动默认"运行中"，不带上次会话的暂停状态
- [x] **版本检查**："检查更新…" 入口（菜单 + 主界面都有），默认查 GitHub Releases API
  - 启动后 5s 在后台检查一次（24h 节流），**只在发现新版本时弹窗**——无更新静默
  - 手动点击触发完整流程（无视 24h 节流，但 6h 手动节流兜底防滥用）
  - 弹窗三按钮：去下载（开浏览器到 release 页）/ 稍后 / 跳过该版本（写 UserDefaults）
  - `YT_UPDATE_REPO=owner/repo` 环境变量覆盖默认仓库
  - 失败（网络 / 解析）一律 `os_log`，不打扰用户

### 4.3 系统日志（已实现）

状态机事件通过 `os_log` 写入系统日志，subsystem `local.yaotong`、category `activity`：

- **状态机事件**（`工作会话开始` / `休息判定` / `超时判定`）按需输出，每次状态切换最多 1 条
- **启动 sentinel** + **休眠 gap** 事件用于确认子系统可达

> 活动类型日志（`鼠标点击` / `键盘按键` …）已**移除**——即便 5s 节流也属高频噪音，状态机事件已覆盖用户需要的信息。

在「控制台.app」按 subsystem 过滤即可看到：

```
Yaotong: [local.yaotong:activity] 腰痛启动 — ...
Yaotong: [local.yaotong:activity] 休息判定：已空闲 10分03秒，重置工作计时器并等待活动
Yaotong: [local.yaotong:activity] 超时判定：已工作 30分00秒，达到工作阈值
```

命令行：

```bash
log show --predicate 'subsystem == "local.yaotong"' --info --last 5m
```

### 4.4 远期功能（P2，不在本次范围）

- 健康数据统计（今日工作时长、休息次数）
- 周报/月报导出
- 与 macOS 专注模式联动

---

## 5. UI 行为详细说明

### 5.1 菜单栏图标

| 状态 | 触发条件 | 图标 |
|------|----------|------|
| 工作中 | 工作计时 < 30 分钟 | 白色 `circle.fill` |
| 超时 | 工作计时 ≥ 30 分钟 | 红色 `circle.fill`（保持红色——警报语义） |

行为：
- 每秒刷新一次反映最新状态
- 进入"超时"立即变红，不闪烁、不弹窗、不发声音
- 休息判定命中后工作清 0（即使在墙钟上）→ 立刻恢复白色
- 点击图标弹出下拉菜单

### 5.2 主界面窗口

应用启动后自动弹出。窗口可被关闭，关闭后通过菜单"显示主界面"或再次点击菜单栏图标重新打开。

```
┌──────────────────────────────────────────┐
│  腰痛                                    │
├──────────────────────────────────────────┤
│  💻 工作计时（自上次休息以来）              │
│  00:23 / 30:00        工作中             │
│  ████░░░░░░░░░░░░░░░░░░░░               │
├──────────────────────────────────────────┤
│  🌙 休息计时（鼠标未动时长）                │
│  00:05 / 10:00        未休息             │
│  ░░░░░░░░░░░░░░░░░░░░░░░                │
├──────────────────────────────────────────┤
│  工作时长  [2] 5  10  15  20  30  45  60  │
│  休息时长  [1] 2  5  10  15  20  30  45  60  │
│  状态: 运行中 / 已暂停                 │
│                              [退出 腰痛] │
└──────────────────────────────────────────┘
```

显示规则：
- **工作计时**：达到阈值时数字变红、文字"已超时"、进度条变红
- **休息计时**：达到阈值时数字变绿、文字"已休息"、进度条变绿
- "等待活动" 状态下工作计时保持 `00:00`，不显示进度
- 修改 Picker 后**立即生效**并持久化

### 5.3 菜单栏下拉菜单

```
─────────────────────────
显示主界面              ← 第一项
─────────────────────────
工作时长:     [30 分钟 ▾]   ← 子菜单
休息时长:     [10 分钟 ▾]   ← 子菜单
─────────────────────────
暂停腰痛                ← 暂停中显示"开始腰痛"
─────────────────────────
重启 腰痛                ← 重启 App
─────────────────────────
退出 腰痛                ← 直接退出，无确认弹窗
─────────────────────────
```

行为：
- 子菜单修改后立即生效并持久化
- "重启 腰痛" 触发应用重启：用 `/usr/bin/open -n` 重新拉起同一个 .app bundle，再 `NSApp.terminate(nil)` 退出当前进程
- "暂停腰痛" / "开始腰痛" 按钮（菜单和主界面同款）根据 `isPaused` 切换标题，**不持久化**（每次启动默认运行中）

---

## 6. 测试需求

### 6.1 单元测试

- 状态机：工作计时累加 / 重置 / 等待活动门 / 超时判定 / 暂停
- 配置：持久化 / 暂停 / 切换
- 事件消息：工作会话启动 / 休息判定 / 超时判定的中文 log 字符串

### 6.2 集成测试（自动化 smoke test）

- `--smoke-test` 模式：构造完整 AppDelegate 链，模拟空闲时间变化驱动 §5.1 / §5.2 行为
- 报告写入 `/tmp/yaotong-smoke.log`，退出码反映是否全过

### 6.3 手动测试清单

- [ ] 首次启动看到菜单栏白色 `circle.fill` 图标
- [ ] 启动后主界面窗口自动弹出
- [ ] 持续使用（默认 30 分钟）后图标变红
- [ ] 离开座位 10+ 分钟后回到工位，图标恢复白色，工作计时从 0 重新开始
- [ ] 回到工位后**必须**有鼠标/键盘活动，工作计时才开始累加
- [ ] 系统休眠 → 唤醒后工作计时从 0 重新开始（必须再次输入才计时）
- [ ] 移动鼠标 / 按键 / 滚轮，在「控制台.app」能看到对应类型的活动日志
- [ ] 点击图标弹出下拉菜单，第一项"显示主界面"
- [ ] 修改工作/休息时长后立即生效（图标刷新 / 主界面数字变化）
- [ ] 重启应用后配置保留
- [ ] "暂停腰痛"后图标停止变化；点击"开始腰痛"恢复
- [ ] 修改工作/休息时长后立即生效（图标刷新 / 主界面数字变化）
- [ ] "退出 腰痛"后菜单栏图标消失（直接退出无确认）

---

## 7. 产品决策记录

### 7.1 已确认的产品决策

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 应用名 | ✅ 腰痛 |
| 2 | 信号选型 | ✅ 仅用 `CGEventSource` 系统能力（不安装事件监听器） |
| 3 | 活动类型识别 | ❌ 不做（判定只看 `idleSeconds < 1.0`） |
| 4 | UI 形式 | ✅ 菜单栏图标（白/红两态）+ 主界面窗口（双计时器 + 设置） |
| 5 | 图标状态 | ✅ 工作中白色 / 超时红色（**只颜色区分**） |
| 6 | 主界面 | ✅ 启动后自动弹出；工作计时在上、休息计时在下 |
| 7 | 工作计时语义 | ✅ 墙钟（鼠标不动也累加）；休息判定命中后清 0 + 挂"等待活动"门 |
| 8 | 休息判定 | ✅ 累计 10 分钟无活动触发 |
| 9 | 强制休息 | ❌ 不做（不锁屏、不弹窗打断） |
| 10 | 暂停功能 | ✅ 做（"暂停腰痛 / 开始腰痛" 纯开关，会议场景；**不持久化**，每次启动默认运行中） |
| 11 | 摄像头/麦克风检测 | ❌ 不做 |
| 12 | 即将超时预警 | ❌ 不做 |
| 13 | 开机自启动 | ❌ 不做 |
| 14 | 图标素材 | ✅ SF Symbol `circle.fill`，18pt |
| 15 | 默认参数 | ✅ 工作 30 分钟 / 休息 10 分钟；菜单里调整 |
| 16 | 工作/休息选项 | ✅ 工作 2/5/10/15/20/30/45/60；休息 1/5/10/15/20/30/45/60 |
| 17 | 应用退出 | ✅ 直接退出（无确认弹窗） |
| 18 | 调试面板 | ❌ 不做（开发者用「系统日志 + 2 分钟测试选项」即可） |
| 19 | 打包方式 | ✅ 本地构建（不签名、不分发） |
| 20 | 数据存储 | ✅ 本地（不上传任何数据） |
| 21 | 开源 | ❌ 不开源 |
| 22 | 重启入口 | ✅ 做（主界面按钮 + 菜单项，"重启 腰痛"） |
| 23 | 菜单无 emoji | ✅ 做（去掉 🪟 / 🔄 / ⏸ / ▶ / 🚪，系统字体下与中文标签不协调） |
| 24 | 休息时长 2 分钟档 | ✅ 做（菜单 + Picker 都有 2 分钟选项） |
| 25 | Dock 图标 | ✅ 做（蓝色填充圆，构建时生成 PNG 烧进 bundle） |
| 26 | 休眠 / 唤醒 | ✅ 做（wall-clock gap + `didWake` 双路，sleep = 休息，唤醒后等输入） |
| 27 | 版本检查 | ✅ 做（GitHub Releases API，后台静默 + 手动弹窗，节流防滥用） |

---

**修订记录（产品）**

完整修订历史见 `git log`。本文档是 2026-06-05 的最终版，反映当前实现：
- 两个独立计时器（工作墙钟 + 休息空闲）
- 休息判定命中后工作清 0 + 挂"等待活动"门
- 菜单栏图标（工作中白色 / 超时红色 `circle.fill`，构造一次缓存复用）
- Dock 图标（蓝色填充圆，1024×1024 PNG 构建时生成烧进 bundle）
- 主界面窗口（启动自动弹出）+ Dock 联动
- 系统日志：状态机事件按需
- "暂停腰痛 / 开始腰痛" 纯开关，不持久化
- 主界面 + 菜单都有"重启 腰痛"；菜单无 emoji
- **版本检查**：菜单 + 主界面都有"检查更新…"，启动后 5s 后台静默检查（24h 节流），手动点击完整结果（6h 节流兜底），默认查 GitHub Releases API，可被 `YT_UPDATE_REPO` 环境变量覆盖
