# 腰痛 - 健康提醒 Mac 应用 产品需求

> 本文档只描述**产品需求**：要做什么、给谁用、什么体验。
> 技术实现、架构选型、文件结构、构建说明见 [TECHNICAL.md](TECHNICAL.md)。

---

## 1. 项目目标

打造一个 macOS 健康提醒应用，帮助长时间办公的用户在持续工作一段时间后主动起身活动，预防久坐带来的健康问题（颈肩腰背、眼睛疲劳等）。

**应用名：腰痛** —— 直白点出久坐对腰部健康的危害，警示感更强。

---

## 2. 核心思路

通过**监控用户输入活动**（不止鼠标移动）来判断工作/休息状态。

### 2.1 可识别的活动信号

| 信号 | 描述 |
|------|------|
| 鼠标移动 | 鼠标位置变化 |
| 键盘按键 | 任意键按下 |
| 鼠标点击 | 左/右/中键 |
| 滚轮滚动 | 上下/左右滚动 |
| 触控板手势 | 多指滑动、捏合等 |
| 屏幕开/关 | 屏幕点亮或熄灭 |
| 系统休眠/唤醒 | 电脑进入睡眠或被唤醒 |

> 屏幕关闭 / 系统休眠 = 用户已离开工位，**视为已充分休息**。

### 2.2 工作/休息判定逻辑

- **工作中**：检测到任一活动信号 → 累加工作时长
- **已休息**：连续 N 分钟（默认 10 分钟）无任何活动信号 → 重置工作计时器
- **超时**：累计工作时长达到阈值（默认 30 分钟）→ 进入"超时"状态

### 2.3 信号采集方案 ✅ 已定：极简方案

只采集"距离上次任意输入事件多少秒"这一个聚合值，覆盖鼠标/键盘/点击/滚动全部输入。

- 不需要计算鼠标移动量
- 不需要申请辅助功能权限
- 不安装全局事件监听器

后续如需精细化（区分输入类型、检测屏幕开关、检测视频会议），可在后续版本迭代。

---

## 3. 核心参数（默认值，可配置）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 工作时长阈值 | 31 分钟 | 连续工作多久后进入"超时"状态 |
| 休息时长阈值 | 11 分钟 | 多长时间的"无活动"视为已休息，重置工作计时 |
| 鼠标活动判定 | 最近 1 秒内有输入即算"活动" | 判定"用户正在操作" |
| 暂停时长 | 1 小时 | "暂停监控"功能的时长（见 4.2） |

> 工作/休息时长需可在配置菜单中调整（见 4.1），可选值 6 / 11 / 16 / 21 / 31 / 46 / 61 分钟。

---

## 4. 功能需求

### 4.1 必须功能（P0）

- [ ] **空闲时间检测**：自动识别用户是否在使用电脑
- [ ] **工作计时**：累计用户连续工作时间
- [ ] **休息判定**：连续 10 分钟无活动 → 重置工作计时器
- [ ] **超时判定**：累计 30 分钟工作 → 进入"超时"状态
- [ ] **菜单栏图标**：常驻 macOS 顶部菜单栏
  - 默认（工作中）：**白色** 图标
  - 超时：**红色** 图标
  - **不做计时，不显示任何文字**，仅靠颜色变化传达状态
- [ ] **点击图标弹出配置菜单**（macOS 标准下拉菜单）
  - 可调整工作时长（6/11/16/21/31/46/61 分钟可选）
  - 可调整休息时长（6/11/16/21/31/46/61 分钟可选）
  - "退出 腰痛" 按钮
- [ ] **配置持久化**：用户设置重启后保留
- [ ] **默认参数支持自定义**：工作/休息时长默认值可由用户随时调整并立即生效

### 4.2 增强功能（P1）

- [ ] **暂停监控**：配置菜单提供"暂停 1 小时"按钮（会议场景）
  - 暂停期间不计时、不变色、不检测活动
  - 暂停结束自动恢复

### 4.2.1 调试面板（P1）

下拉菜单中新增一项 **「🪟 打开调试面板」**（快捷键 ⌘D），打开一个调试窗口，含 3 个 Tab：

| Tab | 用途 |
|-----|------|
| **配置** | 同一份配置的 SwiftUI 视图：工作/休息时长 Picker、暂停/取消暂停、**工作计时实时显示**（MM:SS / MM:SS + 进度条） |
| **测试** | **强制工作中（白）** / **强制超时（红）** / **恢复自动** 三个按钮；**停止日志** / **开始日志** / **清空日志**；**记录 INFO/WARN/ERROR 消息** 三个测试按钮 |
| **日志** | 自动滚动的日志视图，环形缓冲 500 条；`HH:mm:ss.SSS  ℹ️  message` 格式 |

> 调试面板是给开发者 / 高级用户验证应用行为用的；普通用户可以忽略。

### 4.3 远期功能（P2，不在本次范围）

- 区分输入类型（鼠标 vs 键盘 vs 滚动），做工作模式统计
- 健康数据统计（今日工作时长、休息次数）
- 周报/月报导出
- 与 macOS 专注模式联动

---

## 5. UI 行为详细说明

### 5.1 菜单栏图标状态

| 状态 | 触发条件 | 图标颜色 |
|------|----------|----------|
| 工作中（安全） | 工作时长 < 30 分钟 | 白色 |
| 超时 | 工作时长 ≥ 30 分钟 | 红色 |

> **不做任何计时显示，不显示任何文字/数字/emoji**，仅靠图标颜色变化传达状态。

**关键行为**：
- 图标每秒刷新一次，反映最新状态
- 进入"超时"状态时图标立刻变红，不闪烁、不弹窗、不发声音
- 用户休息足够后（10 分钟无活动）图标恢复白色
- 图标点击弹出菜单，菜单中再点击其他项才执行相应动作

### 5.2 配置菜单内容

```
─────────────────────────
工作时长:     [30 分钟 ▾]   ← 下拉选择
休息时长:     [10 分钟 ▾]   ← 下拉选择
─────────────────────────
⏸ 暂停 1 小时              ← 触发暂停（P1）
─────────────────────────
🪟 打开调试面板             ← 打开调试窗口（P1）
🚪 退出 腰痛                ← 关闭应用
─────────────────────────
```

- **下拉选择** 修改后立即生效并持久化
- **"暂停 1 小时"** 选中后菜单项变为"取消暂停"（P1），再次点击可取消暂停
- **"退出 腰痛"** 关闭应用（**直接退出，无确认弹窗**）

### 5.3 调试面板窗口行为

- 窗口可在关闭后通过菜单「🪟 打开调试面板」重新打开（单实例）
- 配置 Tab 中的"工作计时"实时显示当前会话已工作 `MM:SS` / 阈值 `MM:SS`，跨过阈值后计时变红并显示"已超时"
- 测试 Tab 的"强制工作中/超时"按钮立即改变菜单栏图标颜色；"恢复自动"取消覆盖
- 日志 Tab 自动滚动到最新条目

---

## 6. 测试需求

### 6.1 单元测试

- 工作计时累计与重置逻辑
- 状态机切换（工作中 ↔ 超时 ↔ 休息）
- 暂停/恢复逻辑
- 配置项持久化
- 状态机事件输出（工作会话启动/重置/超时）

### 6.2 集成测试（自动化 smoke test）

- 模拟空闲时间变化，验证状态正确转换
- 模拟配置修改，验证菜单项内容实时更新
- 模拟手动覆盖图标状态，验证渲染正确的颜色
- 验证日志 store 的写入和读取

### 6.3 手动测试清单

- [ ] 首次启动能看到菜单栏图标
- [ ] 持续使用 30 分钟后图标变红（无文字、无弹窗）
- [ ] 离开座位 10 分钟后回到工位，图标恢复白色
- [ ] 点击图标弹出配置菜单
- [ ] 修改工作时长后立即生效
- [ ] 重启应用后配置保留
- [ ] "暂停 1 小时"后图标停止变化
- [ ] "退出 腰痛"后菜单栏图标消失（直接退出无确认）
- [ ] 「打开调试面板」弹出 3-Tab 窗口
- [ ] 测试 Tab 的"强制超时"按钮立刻把图标变红
- [ ] 日志 Tab 显示带时间戳的中文事件消息

---

## 7. 产品决策记录

### 7.1 已确认的产品决策

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 应用名 | ✅ 腰痛 |
| 2 | 信号选型 | ✅ 仅用"空闲时间"系统能力（不计算移动量） |
| 3 | UI 形式 | ✅ 菜单栏图标（白/红两态） + 配置菜单 + 调试面板窗口（可选） |
| 4 | 图标状态 | ✅ 工作中白色 / 超时红色（**只颜色区分，不做计时**） |
| 5 | 配置入口 | ✅ 点击图标弹出下拉菜单 |
| 6 | 强制休息 | ❌ 不做 |
| 7 | 暂停功能 | ✅ 做（暂停 1 小时，会议场景） |
| 8 | 摄像头/麦克风检测 | ❌ 不做 |
| 9 | 即将超时预警（P1） | ❌ 不做 |
| 10 | 开机自启动 | ❌ 不做 |
| 11 | 图标素材 | ✅ 系统图标（SF Symbol） |
| 12 | 默认参数 | ✅ 支持自定义（菜单里调整，6/11/16/21/31/46/61 分钟） |
| 13 | 应用退出 | ✅ 直接退出（无确认弹窗） |
| 14 | 调试面板（P1） | ✅ 做（配置 / 测试 / 日志 3 Tab） |

### 7.2 开放的产品问题

> 所有关键产品问题均已确认。

---

**修订记录（产品）**

- 2026-06-05: 初稿创建
- 2026-06-05: 应用名定为"腰痛"
- 2026-06-05: 信号选型定为极简方案
- 2026-06-05: UI 定为菜单栏独占（白色/红色两态 + 配置菜单）
- 2026-06-05: 清理所有技术实现细节，移除"强制休息"功能
- 2026-06-05: 移除摄像头/麦克风检测、即将超时预警、系统通知
- 2026-06-05: 确认系统图标、本地数据、不开源
- 2026-06-05: 菜单栏图标改为"只颜色区分"（去掉超时计时文字）
- 2026-06-05: 移除开机自启动、确认时长选项、确认直接退出、确认本地构建
- 2026-06-05: 移除所有计时显示（配置菜单里的实时状态行删除）
- 2026-06-05: 加入调试面板（覆盖原"无窗口"决策）
- 2026-06-05: 默认值和选项各 +1 分钟（30→31, 10→11, 选项 6/11/16/21/31/46/61），图标缩小到 16pt
